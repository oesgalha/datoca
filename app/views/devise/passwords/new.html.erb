<section class="hero is-fullheight is-info is-bold">
  <div class="hero-body">
    <div class="container">
      <div class="columns is-vcentered">
        <div class="column is-4 is-offset-4">
          <h1 class="title">
            Esqueceu a senha?
          </h1>
          <div class="box has-text-dark">
            <div class="block">
              Preencha com o e-mail da conta:
            </div>
            <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
              <%= f.label :email, class: 'is-hidden' %>
              <p class="control has-icon">
                <%= f.email_field :email, autofocus: true, class: 'input', placeholder: 'e-mail' %>
                <i class="fa fa-envelope"></i>
              </p>
              <hr>
              <p class="control has-text-right">
                <%= f.submit 'Enviar instruções para resetar senha', class: 'button is-success is-fullwidth' %>
              </p>
            <% end %>
          </div>
          <p class="has-text-centered has-text-light">
            <%= link_to "Entrar", new_session_path(resource_name) %><br />
            <%= link_to "Cadastrar", new_registration_path(resource_name) %><br />
            <%= link_to "Voltar para home", root_path %><br />
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
