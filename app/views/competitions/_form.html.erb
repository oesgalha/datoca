<%= form_for(competition) do |f| %>
  <% if competition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(competition.errors.count, "error") %> prohibited this competition from being saved:</h2>

      <ul>
      <% competition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :max_team_size %>
    <%= f.number_field :max_team_size %>
  </div>

  <div class="field">
    <%= f.label :evaluation_type %>
    <%= f.number_field :evaluation_type %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
